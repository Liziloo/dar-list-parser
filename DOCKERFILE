# Dockerfile
FROM python:3.11-slim

WORKDIR /app

# System deps if needed later (for now, pretty minimal)
RUN pip install --no-cache-dir flask pymupdf

COPY original_parser.py virginia_parser.py pdf_to_text.py webapp.py ./

EXPOSE 5000

CMD ["python", "webapp.py"]
