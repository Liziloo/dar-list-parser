[project]
name = "dar-list-parser"
version = "1.0.0"
description = "Parsers for DAR Forgotten Patriots lists (general parser + Virginia variant)"
authors = [{ name = "Liziloo" }]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
dar-parse = "darlistparser.cli:main"

